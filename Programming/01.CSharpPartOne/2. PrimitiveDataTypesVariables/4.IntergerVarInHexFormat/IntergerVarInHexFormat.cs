/* Declare an integer variable and assign it with the 
 * value 254 in hexadecimal format. Use Windows Calculator
 * to find its hexadecimal representation.
 */

using System;

class IntergerVarInHexFormat
{
    static void Main()
    {
        int hexFormat = 0xFE;

        Console.WriteLine(hexFormat);
    }
}

