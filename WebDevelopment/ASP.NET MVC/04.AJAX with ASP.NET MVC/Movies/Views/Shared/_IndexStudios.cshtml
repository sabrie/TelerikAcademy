@model IEnumerable<Movies.Models.Studio>

<table class="table" id="studio-details-table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Address)
        </th>
        <th>
            Actions
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Address)
            </td>
            <td>
                @Ajax.ActionLink("Edit", "Edit", new { id = item.Id },
                    new AjaxOptions
                    {
                        HttpMethod = "GET",
                        InsertionMode = InsertionMode.Replace,
                        UpdateTargetId = "partial-content",
                        OnSuccess = "hideCreateButton"
                    }, new { @class = "btn", id = "edit-studio-button-" + item.Id })
                @Ajax.ActionLink("Details", "Details", new { id = item.Id },
                    new AjaxOptions
                    {
                        HttpMethod = "GET",
                        InsertionMode = InsertionMode.Replace,
                        UpdateTargetId = "partial-content",
                        OnSuccess = "hideCreateButton",
                    }, new { @class = "btn", id = "details-studio-button-" + item.Id })
                @Ajax.ActionLink("Delete", "Delete", new { id = item.Id },
                    new AjaxOptions
                    {
                        HttpMethod = "GET",
                        InsertionMode = InsertionMode.Replace,
                        UpdateTargetId = "partial-content",
                        OnSuccess = "hideCreateButton"
                    }, new { @class = "btn", id = "delete-studio-button-" + item.Id })
            </td>
        </tr>
    }

</table>

